name: G1R2 fast visual

on:
  push:
    branches: [benchmark-v5]
  workflow_dispatch:

jobs:
  capture:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Install test dependencies
        run: npm install --no-audit --no-fund
      - name: Install Chromium
        run: npx playwright install --with-deps chromium
      - name: G1R2 v17.32 syntax gate
        run: node --check g1-benchmark-art-v17-motion.js && node --check g1-benchmark-art-v17-gamefeel.js && node --check qa/g1r2-gamefeel.spec.js
      - name: Capture G1R2 only
        run: npx playwright test --config=playwright.g1r2-fast.config.js
      - name: Upload G1R2 visual
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: g1r2-fast-visual
          path: qa/reports/g1r2-fast/
          if-no-files-found: error
